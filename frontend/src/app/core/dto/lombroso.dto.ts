/** Verdetto del Lombroso Scanner (allineato al record C# LombrosoVerdictDto). */
export interface LombrosoVerdict {
    /** Titolo del "crimine" imputato. */
    title: string;
    /** Falsa motivazione pseudo-antropometrica. */
    desc: string;
}
